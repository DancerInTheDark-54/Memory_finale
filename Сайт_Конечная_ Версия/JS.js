var empty = []; // Пустой массив
  var numbers = [2,3,5,7,11]; // Массив с питью числовыми элементами
  var misc = [1.1,true,"a"]; //3 элемента различных типов + заверщающая запятая
  var base = 1024; 
  var table = [base,base+1,base+2,base+3]; // Массив с переменными 
  var arrObj = [[1,{x:1,y:2}],[2,{x:3,y:4}]]; // 2 массива внутри, содержащие объекты
